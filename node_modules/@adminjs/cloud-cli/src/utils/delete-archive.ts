import { unlink } from 'fs/promises';

const deleteArchive = async (filePath: string) => {
  try {
    await unlink(filePath);
  } catch (error) {
    // ignore error if file no longer exists
  }
};

export default deleteArchive;
