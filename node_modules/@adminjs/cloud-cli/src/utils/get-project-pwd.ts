import { execSync } from 'child_process';

/**
 * Get pwd of project the cli tool was run
 */
const getProjectPwd = (): string => execSync('pwd')
  .toString()
  .replace(/(?:\r\n|\r|\n)/g, '');

export default getProjectPwd;
