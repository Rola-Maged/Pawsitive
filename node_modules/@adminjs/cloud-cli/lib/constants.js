import path from 'path';
import { fileURLToPath } from 'url';
export const PACKAGE_NAME = 'adminjs';
const __filename = fileURLToPath(import.meta.url);
const distPath = path.dirname(__filename);
export const PKG_ROOT = path.join(distPath, '../');
export const FOLDERS_SHOULD_BE_EXCLUDED = ['node_modules', 'test', 'tests'];
export const POSTGRES_DATABASE_URL = 'postgres://adminjs:adminjs@localhost:5435/adminjs';
